@{
    ViewData["Title"] = "Home Page";
}
@model HomeViewModel

<link rel="stylesheet" href="~/css/site.css" />

<section class="hero">
    <section class="hero">
        <img src="~/Icons/bg-word.svg" class="hero-bg-word"/>
        <div class="hero-content">
            <h1>Знайди найкраще місце для відпочинку</h1>
            <p>Швидко. Зручно. Разом із Winbook</p>
        </div>
        <div class="search-panel">
            <div class="search-row full">
                <img src="~/Icons/Home.png" class="icon" />
                <input type="text" placeholder="Де хочете зупинитися?" />
            </div>
            <div class="search-bottom">
                <div class="search-row date-row">
                    <img src="~/Icons/Calendar.png" class="icon calendar-trigger" data-target="checkIn" />
                    <span class="date-text" id="checkInText">Дата заїзду</span>
                    <input type="date" id="checkIn" class="custom-date" />
                </div>
                <div class="search-row date-row">
                    <img src="~/Icons/Calendar.png" class="icon calendar-trigger" data-target="checkOut" />
                    <span class="date-text" id="checkOutText">Дата виїзду</span>
                    <input type="date" id="checkOut" class="custom-date" />
                </div>
                <div class="search-row">
                    <img src="~/Icons/People.png" class="icon"/>
                    <input type="text" placeholder="Кого беремо з собою?" />
                    <button type="button" class="dropdown"><img src="/Icons/dropdown.png" /></button>
                </div>
                <button class="google-acid"><img src="~/Icons/Search.png" /></button>
            </div>
        </div>
    </section>
</section>

<form id="antiForgeryForm">
    @Html.AntiForgeryToken()
</form>

<section class="popular container">
    <div class="popular-head">
        <div>
            <h2>Популярні будинки Парижу</h2>
            <p>Приватні будинки з атмосферою класичного Парижу</p>
        </div>
        <button class="blue more-popular">Знайти більше помешкань</button>
    </div>

    <div class="cards">
        @foreach(var property in Model.Houses){
            <a asp-controller="Home" asp-action="Property" asp-route-id="@property.Id"class="card">

                @if(property.Images.Any()){
                    <img src="@property.Images.First().ImagePath" />
                }
                else{
                    <div class="card-image-placeholder"></div>
                }

                @if(User.Identity?.IsAuthenticated == true)
                {
                    <div class="favorite-button" data-property-id="@property.Id">
                        <img class="favorite-icon" src="@(Model.FavoritePropertyIds.Contains(property.Id) ? Url.Content("~/Icons/heart-fill.png") : Url.Content("~/Icons/heart-null.png"))" />
                    </div>
                }

                <div class="card-info">

                    <h3>@property.Object</h3>

                    <p>@property.City</p>

                    <div class="card-footer">

                        <div class="card-rating">
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/empty-star.png" />
                        </div>

                        <span class="card-price">від @property.PricePerNight₴ за ніч</span>

                    </div>

                </div>
            </a>}

        @for(int i = Model.Houses.Count; i < 4; i++){
            <div class="card card-empty"></div>
        }

    </div>

    <div class="popular-head">
        <div>
            <h2>Найкращі готелі Амстердаму</h2>
            <p>Комфортні готелі біля каналів і в центрі міста</p>
        </div>
        <button class="blue more-popular">Знайти більше помешкань</button>
    </div>

    <div class="cards">
        @foreach(var property in Model.Hotels){
            <a asp-controller="Home" asp-action="Property" asp-route-id="@property.Id"class="card">

                @if(property.Images.Any()){
                    <img src="@property.Images.First().ImagePath" />
                }
                else{
                    <div class="card-image-placeholder"></div>
                }

                @if (User.Identity?.IsAuthenticated == true)
                {
                    <div class="favorite-button" data-property-id="@property.Id">
                        <img class="favorite-icon" src="@(Model.FavoritePropertyIds.Contains(property.Id) ? Url.Content("~/Icons/heart-fill.png") : Url.Content("~/Icons/heart-null.png"))" />
                    </div>
                }

                <div class="card-info">

                    <h3>@property.Object</h3>

                    <p>@property.City</p>

                    <div class="card-footer">

                        <div class="card-rating">
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/empty-star.png" />
                        </div>

                        <span class="card-price">від @property.PricePerNight₴ за ніч</span>

                    </div>

                </div>
            </a>}

        @for(int i = Model.Hotels.Count; i < 4; i++){
            <div class="card card-empty"></div>
        }

    </div>

    <div class="popular-head">
        <div>
            <h2>Атмосферні апартаменти Риму</h2>
            <p>Апартаменти в самому серці історії</p>
        </div>
        <button class="blue more-popular">Знайти більше помешкань</button>
    </div>

    <div class="cards">
        @foreach(var property in Model.Apartments){
            <a asp-controller="Home" asp-action="Property" asp-route-id="@property.Id"class="card">

                @if(property.Images.Any()){
                    <img src="@property.Images.First().ImagePath" />
                }
                else{
                    <div class="card-image-placeholder"></div>
                }

                @if (User.Identity?.IsAuthenticated == true)
                {
                    <div class="favorite-button" data-property-id="@property.Id">
                        <img class="favorite-icon" src="@(Model.FavoritePropertyIds.Contains(property.Id) ? Url.Content("~/Icons/heart-fill.png") : Url.Content("~/Icons/heart-null.png"))" />
                    </div>
                }

                <div class="card-info">

                    <h3>@property.Object</h3>

                    <p>@property.City</p>

                    <div class="card-footer">

                        <div class="card-rating">
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/empty-star.png" />
                        </div>

                        <span class="card-price">від @property.PricePerNight₴ за ніч</span>

                    </div>

                </div>
            </a>}

        @for(int i = Model.Apartments.Count; i < 4; i++){
            <div class="card card-empty"></div>
        }

    </div>

    <div class="popular-head">
        <div>
            <h2>Розкішні віли Барселони</h2>
            <p>Віли поблизу моря та на схилах міста</p>
        </div>
        <button class="blue more-popular">Знайти більше помешкань</button>
    </div>

    <div class="cards">
        @foreach(var property in Model.Villas){
            <a asp-controller="Home" asp-action="Property" asp-route-id="@property.Id"class="card">

                @if(property.Images.Any()){
                    <img src="@property.Images.First().ImagePath" />
                }
                else{
                    <div class="card-image-placeholder"></div>
                }

                @if(User.Identity?.IsAuthenticated == true)
                {
                    <div class="favorite-button" data-property-id="@property.Id">
                        <img class="favorite-icon" src="@(Model.FavoritePropertyIds.Contains(property.Id) ? Url.Content("~/Icons/heart-fill.png") : Url.Content("~/Icons/heart-null.png"))" />
                    </div>
                }

                <div class="card-info">

                    <h3>@property.Object</h3>

                    <p>@property.City</p>

                    <div class="card-footer">

                        <div class="card-rating">
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/star.png" />
                            <img src="~/Icons/empty-star.png" />
                        </div>

                        <span class="card-price">від @property.PricePerNight₴ за ніч</span>

                    </div>

                </div>
            </a>}

        @for(int i = Model.Villas.Count; i < 4; i++){
            <div class="card card-empty"></div>
        }

    </div>
</section>
<script>

    document.querySelectorAll(".favorite-button").forEach(button => {

        button.addEventListener("click", async function (e) {

            e.preventDefault();
            e.stopPropagation();

            const propertyId = this.dataset.propertyId;

            try {

                const response = await fetch("/Home/ToggleFavorite", {

                    method: "POST",

                    headers: {
                        "Content-Type": "application/x-www-form-urlencoded"
                    },

                    body: `propertyId=${propertyId}`

                });

                if(!response.ok) {
                    console.error("Помилка запиту.");
                    return;
                }

                const result = await response.json();

                const icon = this.querySelector(".favorite-icon");

                if(result.isFavorite){

                    icon.src = "/Icons/heart-fill.png";

                }
                else{

                    icon.src = "/Icons/heart-null.png";

                }

            }
            catch(error){

                console.error(error);

            }

        });

    });


    document.addEventListener("DOMContentLoaded", () => {

        const checkIn = document.getElementById("checkIn");
        const checkOut = document.getElementById("checkOut");

        const checkInText = document.getElementById("checkInText");
        const checkOutText = document.getElementById("checkOutText");

        const months = [
            "січня",
            "лютого",
            "березня",
            "квітня",
            "травня",
            "червня",
            "липня",
            "серпня",
            "вересня",
            "жовтня",
            "листопада",
            "грудня"
        ];

        const today = new Date();

        function formatDate(date) {

            const year = date.getFullYear();
            const month = String(date.getMonth() + 1).padStart(2, "0");
            const day = String(date.getDate()).padStart(2, "0");

            return `${year}-${month}-${day}`;
        }

        function formatUa(dateString) {

            if(!dateString) return "";

            const date = new Date(dateString);

            return `${date.getDate()} ${months[date.getMonth()]} ${date.getFullYear()} року`;
        }

        function getNextDay(dateString) {

            const date = new Date(dateString);

            date.setDate(date.getDate() + 1);

            return formatDate(date);
        }

        checkIn.min = formatDate(today);

        checkInText.textContent = "Дата заїзду";
        checkOutText.textContent = "Дата виїзду";

        checkIn.addEventListener("change", () => {

            checkInText.textContent = formatUa(checkIn.value);

            const nextDay = getNextDay(checkIn.value);

            checkOut.min = nextDay;

        });

        checkOut.addEventListener("change", () => {

            checkOutText.textContent = formatUa(checkOut.value);

        });

        document.querySelectorAll(".calendar-trigger").forEach(icon => {

            icon.addEventListener("click", () => {

                const input = document.getElementById(icon.dataset.target);

                if (input.showPicker) {

                    input.showPicker();

                }
                else {

                    input.focus();

                }

            });

        });

    });
</script>